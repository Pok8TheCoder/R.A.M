"""Ported subset of Aryan's `origin/aryan` branch model code.

Only what's needed to load his real trained checkpoint
(`models/checkpoints/aryan_world_model_best.pt`) and run it stand-alone
inside the main repo -- no cross-worktree imports required. See
`docs/WORKSPACE_AND_ARYAN_BRANCH.md` for the full branch comparison.
"""
